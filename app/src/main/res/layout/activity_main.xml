<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.a7yan.servicedemo.MainActivity">

    <Button
        android:id="@+id/btn_on"
        android:text="开启服务"
        android:onClick="open"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/btn_off"
        android:text="关闭服务"
        android:onClick="close"
        android:layout_below="@id/btn_on"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/btn_jump"
        android:text="后台下载服务"
        android:onClick="jump"
        android:layout_below="@id/btn_off"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/btn_jump1"
        android:text="IntentService"
        android:onClick="jump"
        android:layout_below="@id/btn_jump"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <Button
        android:id="@+id/btn_jump2"
        android:text="BoundService"
        android:onClick="jump"
        android:layout_below="@id/btn_jump1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
</RelativeLayout>
